function capitalizeFirstLetter(string) {
    return string.charAt(0).toUpperCase() + string.slice(1);
}

async function getLectionsForCourse(programName, courseName) {
    return console.log("ALLE LECTIONS")
}

module.exports = {
    capitalizeFirstLetter,
    getLectionsForCourse
};
